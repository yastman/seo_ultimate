# GEMINI Instructions

## 1. 🚨 STRICT RULES (CRITICAL)

1.  **COMMIT OFTEN & IMMEDIATELY**:
    *   Делай коммит **сразу** после изменения файла. Не жди выполнения всей задачи.
    *   Формат: `type: description` (например, `feat: add meta for category X`, `fix: correct title description`, `docs: update status`).
    *   **Атомарность**: Один логический шаг = один коммит.

2.  **LANGUAGE**:
    *   Вся коммуникация и мысли — **на русском языке**.
    *   Контент для сайта — **на украинском или русском** (согласно задаче).

3.  **SOURCE OF TRUTH**:
    *   Никогда не придумывай правила SEO. Всегда сверяйся с `docs/CONTENT_GUIDE.md`.
    *   Статус задач всегда в `tasks/PIPELINE_STATUS.md`.

## 2. 🛠️ Environment & Scripts

**Python**: Используй `.venv`. Если нужно, активируй или указывай путь к python из venv.

### ⚡ Quick Script Reference
Запомни эти команды наизусть. Используй их для проверки своей работы.

| Задача | Скрипт | Пример вызова |
|---|---|---|
| **Валидация Meta** | `validate_meta.py` | `python scripts/validate_meta.py categories/{slug}/meta/{slug}_meta.json` |
| **Валидация Content** | `validate_content.py` | `python scripts/validate_content.py categories/{slug}/content/{slug}_ru.md "{keyword}" --mode seo` |
| **Анализ Категории** | `analyze_category.py` | `python scripts/analyze_category.py {slug}` |
| **HTML Превью** | `md_to_html.py` | `python scripts/md_to_html.py categories/{slug}/content/{slug}_ru.md` |
| **Загрузка в БД** | `upload_to_db.py` | `python scripts/upload_to_db.py {slug}` (Требует PASS валидации) |

## 3. 🔄 Workflow (Skills)

Когда пользователь просит выполнить этап пайплайна, выполняй соответствующую последовательность:

### 🔹 `/category-init {slug}`
1.  Проверь, есть ли папка `categories/{slug}`.
2.  Если нет — создай структуру: `data/`, `meta/`, `content/`, `research/`.
3.  Создай `_clean.json` (ключевые слова).
4.  **COMMIT**.

### 🔹 `/generate-meta {slug}`
1.  Читайте `_clean.json`.
2.  Генерируй Title/Description/H1 согласно `docs/CONTENT_GUIDE.md`.
    *   *Title*: 50-60 chars, "Купить", Бренд в конце.
    *   *Description*: 120-160 chars, без смайлов, коммерция.
    *   *H1*: Без "Купить", отличается от Title.
3.  Сохрани в `meta/{slug}_meta.json`.
4.  **VALIDATE**: Запусти `validate_meta.py`.
5.  **COMMIT**.

### 🔹 `/seo-research {slug}`
1.  Анализируй конкурентов (из головы или используя `search_web` если доступно).
2.  Заполни `research/RESEARCH_DATA.md`:
    *   Intro intent, User problems, FAQ candidates, Semantic core priorities.
3.  **COMMIT**.

### 🔹 `/content-generator {slug}`
1.  Используй данные из Research и Meta.
2.  Пиши контент в `content/{slug}_ru.md`.
    *   *Intro*: 30-60 слов.
    *   *Tables*: Обязательно.
    *   *Structure*: Intro -> How to choose -> How to use -> Errors -> FAQ.
3.  **VALIDATE**: Запусти `validate_content.py`.
4.  **COMMIT**.

## 4. 📂 Project Structure Map

*   `tasks/PIPELINE_STATUS.md` — **ЧИТАЙ В ПЕРВУЮ ОЧЕРЕДЬ**. Очередь задач.
*   `tasks/categories/{slug}.md` — Чеклист конкретной категории. Отмечай `[x]` по ходу.
*   `docs/CONTENT_GUIDE.md` — Библия SEO. Нарушение правил = провал.
*   `scripts/` — Набор инструментов. Не пиши свои велосипеды, ищи готовый скрипт.
*   `CLAUDE.md` — Старший брат. Там глобальная логика.

## 5. ⚠️ Common Pitfalls (Не делай этого)
*   Не меняй статус статуса в `PIPELINE_STATUS.md` на ✅, пока не прошла валидация скриптом.
*   Не забывай `python` перед запуском скриптов (Windows environment).
*   Не пиши H1 "Купить {товар}". Читатель уже на сайте, ему не надо продавать клик.

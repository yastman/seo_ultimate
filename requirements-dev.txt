# Compatibility snapshot for pip-based development environments.
# The canonical dependency source is pyproject.toml + uv.lock.

-r requirements.txt

pytest>=9.0
pytest-cov>=7.0
pytest-xdist>=3.5
ruff>=0.14
mypy>=1.18
mysql-connector-python>=9.0
pre-commit>=4.0

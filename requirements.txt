# Compatibility snapshot for pip-based environments.
# The canonical dependency source is pyproject.toml + uv.lock.

pyyaml>=6.0
tqdm>=4.67
requests>=2.32
toml>=0.10
pandas>=2.2
markdown>=3.8
openpyxl>=3.1
setuptools<71

# 🆘 TROUBLESHOOTING GUIDE

**Решение проблем, частые ошибки и debug чек-листы**

---

## 📚 Содержание

1. [FAQ — Часто задаваемые вопросы](#faq)
2. [Ошибки по Stage](#ошибки-по-stage)
3. [Debug чек-листы](#debug-чек-листы)
4. [Performance issues](#performance-issues)
5. [Контакты для помощи](#контакты-для-помощи)

---

## ❓ FAQ

### Q: С чего начать новую категорию?

**A:** Прочитайте [`QUICK_START.md`](QUICK_START.md) и выполните:

```
"активная пена полный workflow"
```

Замените "активная пена" на название вашей категории (slug).

---

### Q: Что такое slug?

**A:** Машиночитаемое название категории. Примеры:

| Человеческое | Slug |
|---|---|
| Активная пена | aktivnaya-pena |
| Стиральный порошок | stiralniy-poroshok |
| Средство для посуды | sredstvo-dlya-posudy |

Правила: только латиница, дефисы, строчные буквы, 50 символов макс.

Подробнее: [`GLOSSARY.md`](GLOSSARY.md#slug)

---

### Q: Как узнать статус workflow?

**A:** Смотрите логи в `categories/{slug}/.logs/`:

```bash
# Последние события
tail -f categories/{slug}/.logs/*.log

# Конкретный агент
tail categories/{slug}/.logs/content-generation-agent.log
```

---

### Q: Где мои результаты?

**A:** После успешного workflow в папке:

```
categories/{slug}/deliverables/
├─ {slug}_ru.md          # РУ контент
├─ {slug}_uk.md          # UA перевод
├─ {slug}_meta.json      # Meta tags
├─ README.md             # Инструкции
└─ QUALITY_REPORT.md     # Отчет качества
```

---

### Q: Workflow зависает или не запускается

**A:** Проверьте:

1. **Правильный slug?**
   ```bash
   ls categories/
   # Должна быть папка categories/{slug}/
   ```

2. **Логи есть?**
   ```bash
   ls categories/{slug}/.logs/
   # Должны быть файлы *.log
   ```

3. **На какой stage застрял?**
   ```bash
   cat task_{slug}.json | grep "current_stage"
   ```

Подробнее: [`Debug чек-листы`](#debug-чек-листы)

---

### Q: Как возобновить workflow после ошибки?

**A:** Дайте команду, указав правильный stage:

```
"продолжи для {slug}"
```

Система прочитает `task_{slug}.json` и продолжит с последнего stage.

---

### Q: Что если контент слишком короткий?

**A:** Требование: 4000–5000 символов (без пробелов).

**Решение:**
1. Проверьте `QUALITY_REPORT.md` → секция "Content Length"
2. Расширьте контент H2 блоками или FAQ
3. Запустите: `"сгенерируй контент для {slug}"`

---

### Q: Keyword coverage < 70%

**A:** Проверьте:

1. **Файл с keywords:**
   ```bash
   cat categories/{slug}/data/{slug}_keywords_distributed.json
   ```

2. **Есть ли keywords в контенте?**
   ```bash
   grep -o "keyword" categories/{slug}/content/{slug}_ru.md | wc -l
   ```

3. **Возможные причины:**
   - Keywords слишком узкие (только "пена", а не "активная пена")
   - Keywords конфликтуют друг с другом
   - Контент не соответствует ТЗ от Perplexity

**Решение:** Запросите новое ТЗ в Perplexity с более релевантными keywords

---

### Q: Meta title/description имеет неправильную длину

**A:** Требования:
- **Meta Title:** 50–70 символов
- **Meta Description:** 140–170 символов

**Решение:**
1. Проверьте файл:
   ```bash
   cat categories/{slug}/meta/{slug}_meta.json
   ```

2. Запустите:
   ```
   "сгенерируй контент для {slug}"
   ```

3. Meta tags пересчитаются автоматически

---

### Q: Украинский перевод неестественный

**A:** Перевод должен быть **натуральным украинским**, не калькой с русского.

**Проверка:**
```bash
# Сравните RU и UK версии
diff -u categories/{slug}/content/{slug}_ru.md \
         categories/{slug}/content/{slug}_uk.md | head -50
```

**Если проблема остается:**

1. Запустите перевод заново:
   ```
   "сгенерируй контент для {slug}"
   ```

2. Сообщите об ошибке (если систематическая)

---

## ⚠️ Ошибки по Stage

### Stage -3: URL Extraction

#### ❌ Ошибка: "SERP CSV not found"

**Причина:** Файл `data/input/поисковая_выдача_топ_10.csv` не найден

**Решение:**
```bash
# Проверьте файл
ls -la data/input/поисковая_выдача_топ_10.csv

# Если не существует, создайте или загрузите
# Требуемый формат: URL, keyword, ...
```

#### ❌ Ошибка: "urls_raw.txt is empty"

**Причина:** CSV файл не содержит URLs

**Проверка:**
```bash
head data/input/поисковая_выдача_топ_10.csv
# Должны быть URLs в первом столбце
```

#### ❌ Валидатор FAIL: "Less than 8 URLs"

**Причина:** Извлекли < 8 URLs

**Решение:**
```bash
# Посмотрите логи
cat categories/{slug}/.logs/url-extraction-agent.log

# Добавьте больше keywords в SERP CSV
# Или выберите более популярную категорию
```

---

### Stage -2: URL Preparation

#### ❌ Ошибка: "urls.txt is empty"

**Причина:** После фильтрации осталось < 5 URLs

**Проверка:**
```bash
# Сколько URLs в urls_raw.txt?
wc -l categories/{slug}/urls_raw.txt

# Какие отфильтровали?
cat categories/{slug}/.logs/url-preparation-agent.log
```

#### ❌ Валидатор FAIL: "Duplicate /ua/ prefix"

**Причина:** URLs содержат `/ua/` (украинская версия)

**Решение:**
```bash
# Проверьте файл
grep "/ua/" categories/{slug}/urls.txt

# Удалите строки с /ua/, оставьте только основные URLs
# Затем повторите stage -2
```

#### ❌ Валидатор FAIL: "HTTP instead of HTTPS"

**Причина:** URLs используют `http://` вместо `https://`

**Решение:**
```bash
# Замените все http на https
sed -i 's/http:\/\//https:\/\//g' categories/{slug}/urls.txt

# Повторите stage -2
```

---

### Stage 3: Manual Research

#### 🛑 PAUSE: "Waiting for meta_competitors.csv and perplexity_research.md"

**Что делать:**

**A. Screaming Frog (2 минуты)**

1. Откройте `categories/{slug}/urls.txt`
2. Загрузите в Screaming Frog
3. Экспортируйте (Crawl → Export):
   - URL
   - Title (Page Title)
   - H1 (H1)
   - Meta Description
4. Сохраните CSV: `categories/{slug}/competitors/meta_competitors.csv`

**Проверка формата:**
```bash
head -5 categories/{slug}/competitors/meta_competitors.csv
# Должны быть 4 столбца: URL, Title, H1, Meta Description
```

**B. Perplexity (3 минуты)**

1. Откройте [Perplexity.ai](https://perplexity.ai)
2. Создайте ТЗ:
   ```
   Проанализируй top-10 конкурентов для "{slug}".
   Предложи структуру контента:
   - H2 блоки (2-4)
   - FAQ вопросы (3-6)
   - Ключевые темы

   Результат: markdown формат с разделами
   ```
3. Сохраните: `categories/{slug}/research/perplexity_research.md`

**Проверка:**
```bash
# Файл должен содержать H2, FAQ, другие структурные элементы
wc -l categories/{slug}/research/perplexity_research.md
# Минимум 50 строк
```

#### ❌ Ошибка: "meta_competitors.csv has wrong format"

**Решение:**

1. Проверьте столбцы:
   ```bash
   head -1 categories/{slug}/competitors/meta_competitors.csv
   # Должны быть: URL,Title,H1,Meta Description (без пробелов)
   ```

2. Если неправильно, отредактируйте в Excel или пересоздайте в Screaming Frog

---

### Stage 4: Data Preparation

#### ❌ Ошибка: "No meta_competitors.csv found"

**Причина:** Не завершена MANUAL STAGE 3

**Решение:** Смотрите раздел выше "Stage 3: Manual Research"

#### ❌ Ошибка: "JSON parsing error"

**Причина:** `data/{slug}.json` имеет неправильный формат

**Проверка:**
```bash
python -m json.tool categories/{slug}/data/{slug}.json
# Если ошибка, файл поврежден
```

**Решение:** Удалите JSON и пересоздайте:
```bash
rm categories/{slug}/data/{slug}.json
"продолжи для {slug}"
```

---

### Stage 6: Keyword Distribution

#### ⚠️ Валидатор WARN: "Coverage < 70%"

**Причина:** Keywords недостаточно распределены по контенту

**Проверка:**
```bash
cat categories/{slug}/data/{slug}_keywords_distributed.json | grep coverage
```

**Решение:**
1. Проверьте ТЗ от Perplexity (может быть неправильно)
2. Пересоздайте ТЗ с более релевантными keywords
3. Запустите stage 8 заново

#### ⚠️ Валидатор WARN: "Keyword density > 2%"

**Причина:** Ключевое слово повторяется слишком часто (спам)

**Проверка:**
```bash
python scripts/show_keyword_distribution.py \
    categories/{slug}/data/{slug}_keywords_distributed.json
```

**Решение:** Используйте синонимы, варианты phrases

---

### Stage 8: Content Generation

#### ❌ Ошибка: "Content length < 4000 characters"

**Причина:** Контент слишком короткий

**Проверка:**
```bash
wc -c categories/{slug}/content/{slug}_ru.md
# Минимум 4000 символов (без пробелов)
```

**Решение:**
```
"сгенерируй контент для {slug}"
```

Система пересчитает с учетом требований.

#### ❌ Ошибка: "No H1 found in content"

**Причина:** Контент не имеет заголовков

**Проверка:**
```bash
grep "^#" categories/{slug}/content/{slug}_ru.md
```

**Решение:** ТЗ от Perplexity должна содержать H2 блоки

---

### Stage 9: Ukrainian Translation

#### ❌ Ошибка: "Translation quality issues"

**Проверка:**
```bash
# Сравните длины
wc -c categories/{slug}/content/{slug}_ru.md
wc -c categories/{slug}/content/{slug}_uk.md

# Допустимо ±5%
```

**Если различие > 5%:** Пересчитайте перевод

#### ❌ Ошибка: "Invalid HTML tags"

**Причина:** Markdown содержит неправильные теги

**Проверка:**
```bash
grep "<" categories/{slug}/content/{slug}_uk.md
# Проверьте, что все теги закрыты
```

---

### Stage 10: Meta Tags

#### ❌ Ошибка: "Meta title too short/long"

**Требование:** 50–70 символов

**Решение:**
```
"сгенерируй контент для {slug}"
```

#### ❌ Ошибка: "Meta description too short/long"

**Требование:** 140–170 символов

**Решение:** Как выше

---

### Stage 11: Packaging

#### ❌ Ошибка: "deliverables/ folder not found"

**Причина:** Stage 11 не завершена

**Проверка:**
```bash
ls -la categories/{slug}/deliverables/
# Должны быть 5 файлов
```

**Решение:**
```
"упакуй deliverables для {slug}"
```

---

## 🔧 Debug чек-листы

### Универсальный чек-лист

- [ ] Категория создана: `ls categories/{slug}/`
- [ ] Task файл существует: `cat task_{slug}.json`
- [ ] На каком stage застрял: `grep "current_stage" task_{slug}.json`
- [ ] Логи доступны: `ls categories/{slug}/.logs/`
- [ ] JSON файлы валидны: `python -m json.tool categories/{slug}/data/*.json`
- [ ] Файлы MD существуют: `ls categories/{slug}/content/`
- [ ] Deliverables готовы: `ls categories/{slug}/deliverables/`

### Для URL проблем

- [ ] SERP CSV существует: `ls data/input/поисковая_выдача_топ_10.csv`
- [ ] URLs извлечены: `wc -l categories/{slug}/urls_raw.txt` (≥8)
- [ ] URLs отфильтрованы: `wc -l categories/{slug}/urls.txt` (≥5)
- [ ] Нет /ua/ префиксов: `grep "/ua/" categories/{slug}/urls.txt` (должно быть пусто)
- [ ] Все HTTPS: `grep "http://" categories/{slug}/urls.txt` (должно быть пусто)

### Для контента

- [ ] Длина OK: `wc -c categories/{slug}/content/{slug}_ru.md` (4000–5000)
- [ ] Coverage OK: `grep "coverage" categories/{slug}/data/{slug}_keywords_distributed.json` (≥70%)
- [ ] H2 есть: `grep "^##" categories/{slug}/content/{slug}_ru.md` (2–4 штуки)
- [ ] FAQ есть: `grep "^## Q:" categories/{slug}/content/{slug}_ru.md` (3–6 штук)
- [ ] Meta Title OK: `grep "title" categories/{slug}/meta/{slug}_meta.json` (50–70)
- [ ] Meta Desc OK: `grep "description" categories/{slug}/meta/{slug}_meta.json` (140–170)

### Для quality

```bash
# Запустить полную проверку Stage 8.1
python scripts/quality_runner.py \
    categories/{slug}/content/{slug}_ru.md \
    "keyword phrase" \
    B

# Посмотреть отчет
cat categories/{slug}/content/*_quality_report.json
```

---

## ⚡ Performance Issues

### Workflow работает медленно

**Причины:**
1. Большой JSON файл (>50 МБ)
2. Много URLs (>100)
3. Длинный контент (>10000 символов)

**Решение:**
```bash
# Для больших файлов используйте потоковую обработку
python scripts/check_simple_v2_md.py \
    categories/{slug}/content/{slug}_ru.md \
    "keyword" \
    B \
    --streaming
```

### Memory issues

**Проверка:**
```bash
free -h
# Должно быть достаточно памяти
```

**Решение:**
- Обработайте категории по одной
- Остановите другие процессы
- Используйте `--no-cache` флаг

---

## 📞 Контакты для помощи

### Перед тем как писать

1. **Прочитайте FAQ выше** — в 90% случаев ответ есть
2. **Проверьте логи:**
   ```bash
   tail -100 categories/{slug}/.logs/*.log
   ```
3. **Смотрите QUICK_START:**
   [`QUICK_START.md`](QUICK_START.md)

### Информация для отчета об ошибке

Если ошибка не решена, предоставьте:

1. **Команду, которую вы выполнили:**
   ```
   "активная пена полный workflow"
   ```

2. **Лог из категории:**
   ```bash
   tar -czf logs.tar.gz categories/{slug}/.logs/
   ```

3. **Task файл:**
   ```bash
   cat task_{slug}.json
   ```

4. **На каком stage ошибка:**
   ```
   Stage X: Agent Y
   Error: [сообщение об ошибке]
   ```

5. **Что вы пытались:**
   ```
   Попробовал [действие], получил [ошибка]
   ```

### Документация для дальнейшего изучения

- [`QUICK_START.md`](QUICK_START.md) — С чего начать
- [`GLOSSARY.md`](GLOSSARY.md) — Объяснение терминов
- [`WORKFLOW_DIAGRAM.md`](WORKFLOW_DIAGRAM.md) — Как это работает
- [`DEVELOPER_GUIDE.md`](DEVELOPER_GUIDE.md) — Для расширения
- [`CLAUDE.md`](CLAUDE.md) — Полные инструкции

---

**Версия:** 1.0 | **Updated:** 2025-11-17


# Archive

**[← Назад в корень](../README.md)**

Хранилище устаревших скриптов, документов и данных.
Файлы здесь сохраняются для истории, но **не используются** в активном пайплайне.

---

## 📂 Содержимое

-   **`scripts/`**: Старые версии скриптов (`analyze_categories.py`, `audit_project_full.py` и др.), замененные новыми инструментами в `scripts/`.
-   **`legacy_data/`**: Старые CSV и JSON выгрузки.
-   **`tasks/`**: Выполненные задачи и старые ТЗ (перемещены из корня `tasks/files`).

---

## ⚠️ Внимание

Не используйте код из этой директории в продакшене. Он может содержать:

-   Устаревшие импорты
-   Неактуальную логику
-   Ошибки, исправленные в новых версиях

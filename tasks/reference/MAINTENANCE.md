# Maintenance Guide — Task System

Централизованный гайд по поддержке системы задач проекта.

---

## Добавление новой категории

### 1. Создать чеклист категории

```bash
cp tasks/TEMPLATE_CATEGORY.md tasks/categories/{slug}.md
```

Заменить плейсхолдеры: `{slug}`, `{Name}`, `{parent-slug}`, `XXX`, `YYYY-MM-DD`

Шаблон: [TEMPLATE_CATEGORY.md](TEMPLATE_CATEGORY.md)

### 2. Добавить в MASTER_CHECKLIST.md

Добавить строку в таблицу "Все категории по этапам":

```markdown
| {N} | {slug} | ⬜ | ⬜ | ⬜ | ⬜ | ⬜ | ⬜ | ⬜ |
```

Обновить счётчик в шапке:

```markdown
**Total Categories:** {N} ({N-1} существующих + 1 новая)
```

### 3. Обновить PIPELINE_STATUS.md

В секции "Progress Overview" обновить Total:

```markdown
| 01-init | /category-init | XX/YY ✅ | ...
```

Если HIGH priority — добавить в "Current Queue".

---

## Обновление статусов

### При выполнении этапа

1. **В чеклисте категории** (`tasks/categories/{slug}.md`):
   - Отметить `[x]` выполненные задачи
   - Изменить статус этапа: `⬜ → ✅`
   - Обновить таблицу Current Status

2. **В MASTER_CHECKLIST.md**:
   - Найти строку категории
   - Изменить символ в соответствующей колонке: `⬜ → ✅`

3. **В PIPELINE_STATUS.md**:
   - Обновить Progress Overview (счётчики X/Y)

### Формулы для счётчиков

**Progress Overview (PIPELINE_STATUS.md):**

| Колонка | Формула |
|---------|---------|
| RU | COUNT(MASTER_CHECKLIST где Stage=✅) |
| UK | COUNT(uk/categories/{slug}/ существует И Stage=✅) |
| Pending | Total - RU |

**Сводка по этапам (MASTER_CHECKLIST.md):**

| Статус | Формула |
|--------|---------|
| Готово | COUNT(✅) в колонке |
| В работе | COUNT(🔄) в колонке |
| Ожидает | COUNT(⬜) в колонке |
| Не создано | COUNT(❌) в колонке |

**UK-специфика:**

- UK Init = количество папок в `uk/categories/`
- UK Meta = категории с `uk/categories/{slug}/meta/{slug}_meta.json`
- UK Content = категории с `uk/categories/{slug}/content/{slug}_uk.md`

---

## Пример: обновление статуса

**Сценарий:** категория `tverdyy-vosk` завершила Meta, переходит к Research

### Шаг 1: Обновить чеклист категории

`tasks/categories/tverdyy-vosk.md`:

```diff
  ## Current Status

  | Stage | RU | UK |
  |-------|----|----|
  | 01-Init | ✅ | ⬜ |
- | 02-Meta | ⬜ | ⬜ |
+ | 02-Meta | ✅ | ⬜ |
  | 03-Research | ⬜ | — |

  ---

- ## Stage 02: Meta ⬜
+ ## Stage 02: Meta ✅

  ### Tasks RU
- - [ ] title_ru: 50-60 chars
+ - [x] title_ru: 50-60 chars
  ...
```

### Шаг 2: Обновить MASTER_CHECKLIST.md

```diff
- | 35 | tverdyy-vosk | ✅ | ⬜ | ⬜ | ⬜ | ⬜ | ⬜ | ⬜ |
+ | 35 | tverdyy-vosk | ✅ | ✅ | ⬜ | ⬜ | ⬜ | ⬜ | ⬜ |
```

Обновить сводку:

```diff
  | Этап | Готово | В работе | Ожидает |
  |------|--------|----------|---------|
- | 02-Meta | 34 | 0 | 24 |
+ | 02-Meta | 35 | 0 | 23 |
```

### Шаг 3: Обновить PIPELINE_STATUS.md

```diff
  | Stage | Skill | RU | UK | Pending |
  |-------|-------|----|----|---------|
- | 02-meta | /generate-meta | 34/58 | 34/58 | 24 |
+ | 02-meta | /generate-meta | 35/58 | 34/58 | 23 |
```

---

## Правила работы с задачами

### После выполнения этапа — ОБЯЗАТЕЛЬНО

1. **Сразу** отметить `[x]` в чеклисте категории
2. **Сразу** обновить статус `⬜ → ✅` в MASTER_CHECKLIST
3. **Сразу** пересчитать счётчик в PIPELINE_STATUS

> ⚠️ НЕ откладывать на потом. Делать сразу после завершения этапа.

### После обнаружения проблемы

1. Создать fix в `tasks/fixes/`
2. Добавить в Fixes Queue в PIPELINE_STATUS
3. Приоритизировать (HIGH если блокирует другие задачи)

### После добавления новой категории в CSV

1. Запустить `/category-init {slug}`
2. Создать `tasks/categories/{slug}.md` по шаблону
3. Добавить строку в MASTER_CHECKLIST
4. Обновить Total Categories
5. Добавить в Current Queue если HIGH priority

### После batch-работы (5+ категорий за сессию)

1. Пересчитать ВСЕ счётчики в Progress Overview
2. Обновить секции "Categories by Status"
3. Проверить синхронизацию (Quick Commands)
4. Обновить дату Updated во всех изменённых файлах

### После завершения сессии работы

- [ ] Все выполненные задачи отмечены
- [ ] Счётчики актуальны
- [ ] Current Queue отражает следующие шаги

---

## Синхронизация PIPELINE_STATUS ↔ MASTER_CHECKLIST

### Правило единого источника истины

| Данные | Источник истины |
|--------|-----------------|
| Статус конкретного этапа | `tasks/categories/{slug}.md` |
| Сводная таблица | `MASTER_CHECKLIST.md` |
| Счётчики прогресса | `PIPELINE_STATUS.md` |

### Порядок обновления

```
1. categories/{slug}.md  — детальная работа
         ↓
2. MASTER_CHECKLIST.md   — символ ⬜→✅
         ↓
3. PIPELINE_STATUS.md    — пересчитать X/Y
```

### Проверка синхронизации

```bash
# Проверить количество категорий
ls tasks/categories/*.md | wc -l  # должно = Total в MASTER_CHECKLIST

# Проверить счётчики Meta (пример)
grep "| ✅ |" tasks/MASTER_CHECKLIST.md | wc -l
```

---

## Добавление нового Fix

### Создание задачи исправления

Файл: `tasks/fixes/{issue-name}.md`

```markdown
# Fixes: {Issue Name}

**Status:** PENDING
**Priority:** HIGH/MEDIUM/LOW

---

## Issue {ID}: {Short description}

### Problem
{Описание проблемы}

### Current State
```

{Текущее состояние}

```

### Solution
{Предлагаемое решение}

### Checklist
- [ ] {Шаг 1}
- [ ] {Шаг 2}
- [ ] Валидировать

### Validation
```bash
{команда валидации}
```

---

## Status Tracking

| Issue | Status | Fixed Date |
|-------|--------|------------|
| {ID} | ⬜ PENDING | — |

---

**Last Updated:** YYYY-MM-DD

```

### Добавление в очередь

В `PIPELINE_STATUS.md` секция "Fixes Queue":
```markdown
- [{ID}: {description}](fixes/{issue-name}.md) — краткое описание
```

---

## Валидация целостности Task System

### Ручная проверка

```bash
# 1. Количество категорий совпадает
CATS=$(ls tasks/categories/*.md | wc -l)
echo "Categories files: $CATS"

# 2. Все slug уникальны
ls tasks/categories/*.md | xargs -n1 basename | sort | uniq -d

# 3. Счётчики в PIPELINE_STATUS корректны
# (визуально сравнить с MASTER_CHECKLIST)
```

### Автоматическая валидация (будущее)

```bash
# TODO: создать скрипт
python3 scripts/validate_tasks.py
```

**Что проверять:**

- [ ] Количество файлов в `tasks/categories/` = Total в MASTER_CHECKLIST
- [ ] Каждый slug из MASTER_CHECKLIST имеет файл
- [ ] Счётчики в Progress Overview = подсчёту из таблицы MASTER
- [ ] Нет "висячих" ссылок на несуществующие категории

---

## Типичные ошибки

| Ошибка | Решение |
|--------|---------|
| Забыли обновить MASTER_CHECKLIST | После каждого этапа — сразу обновлять |
| Счётчики не совпадают | Пересчитать вручную по таблице |
| Дубли в чеклисте | Проверить slug уникальность |
| Категория без файла | Создать по шаблону выше |

---

## Quick Commands

```bash
# Создать новую категорию (структура папок)
/category-init {slug}

# Сколько категорий в системе
ls tasks/categories/*.md | wc -l

# Найти категории без Meta
grep "| ⬜ |" tasks/MASTER_CHECKLIST.md | head -20

# Категории готовые к Deploy
grep "| ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ⬜ |" tasks/MASTER_CHECKLIST.md
```

---

**Version:** 1.2
**Created:** 2025-12-31

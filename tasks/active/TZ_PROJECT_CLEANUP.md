# ТЗ: Глобальная очистка и реструктуризация проекта Ultimate SEO

**Дата:** 2026-01-05
**Статус:** 📝 Планирование
**Цель:** Привести структуру проекта в порядок, удалить мусор, консолидировать инструменты.

---

## 1. 🧹 Очистка корневой директории (Garbage Collection)

В корне проекта скопилось много временных файлов и логов.

### Действие: Удалить / Переместить

| Файл / Папка                 | Действие       | Куда               | Причина             |
| ---------------------------- | -------------- | ------------------ | ------------------- |
| `lint_report.txt`            | ❌ Удалить     | -                  | Временный лог       |
| `mypy_errors.txt`            | ❌ Удалить     | -                  | Временный лог       |
| `ruff-log.txt`               | ❌ Удалить     | -                  | Временный лог       |
| `analysis.log`               | ❌ Удалить     | -                  | Временный лог       |
| `check_csv_temp.py`          | ❌ Удалить     | -                  | Одноразовый скрипт  |
| `env_test.py`                | ❌ Удалить     | -                  | Тестовый файл       |
| `ultimate_net_ua_backup.sql` | 📦 Переместить | `data/dumps/`      | Дамп БД             |
| `content_updates.sql`        | 📦 Переместить | `data/sql_output/` | Результат генерации |

### Действие: Игнорировать

Добавить в `.gitignore`:

```gitignore
*.log
*.txt
*.sql
!requirements.txt
!CMakeLists.txt
temp/
.logs/
```

---

## 2. 🛠 Рефакторинг скриптов (Script Consolidation)

**Подробный план в файле:** `tasks/TZ_SCRIPTS_REFACTORING.md`

### Краткая суть:

1.  **Уменьшение количества скриптов**: С 52 до ~36.
2.  **Удаление дублей**: `quality_runner.py`, `audit_content.py` и др.
3.  **Объединение по доменам**:
    -   `synonym_tools.py` (чистка, анализ, отчеты)
    -   `competitors.py` (экстракция, фильтрация)
    -   `products.py` (работа с SQL и товарами)
4.  **Единая точка входа**: Стремиться к запуску через `python -m src.tools.name` (в будущем).

---

## 3. 📂 Реструктуризация папок (Directory Structure)

Текущая структура слишком плоская в `scripts/` и `tasks/`.

### Предлагаемая структура (Target):

```base
Ultimate_SEO/
├── categories/             # [NO CHANGE] 280+ папок категорий
├── data/                   # Данные проекта
│   ├── raw/                # Исходные CSV, SQL дампы
│   ├── generated/          # STRUCTURE.md, JSON отчеты
│   └── dictionaries/       # Словари (синонимы, стоп-слова)
├── docs/                   # Документация
│   ├── standards/          # CONTENT_GUIDE, RESEARCH_GUIDE
│   └── legacy/             # Старые доки (архив)
├── scripts/                # Инструменты (пока оставляем имя, меняем нутро)
│   ├── lib/                # Shared modules (seo_utils.py, config.py)
│   ├── validators/         # Скрипты проверок (validate_*)
│   ├── generators/         # Скрипты генерации (md_to_html, sql)
│   ├── analytics/          # Скрипты анализа
│   └── migrations/         # Одноразовые трансформации
├── tasks/                  # Управление задачами
│   ├── active/             # Текущие активные ТЗ
│   ├── archive/            # Выполненные ТЗ
│   └── reports/            # Генерируемые отчеты
└── tests/                  # Тесты (pytest)
```

**Этап 1:** Только очистка корневых файлов и рефакторинг скриптов внутри текущей папки `scripts/`.
**Этап 2:** Группировка скриптов по подпапкам (`validators`, `generators`) — **Осторожно**, потребует правки импортов!

---

## 4. 📝 План работ (Roadmap)

1.  **Утверждение**: Пользователь подтверждает удаление мусора из корня.
2.  **Очистка**: Удаляем `.txt`, `.log`, перемещаем SQL дампы в `data/dumps`.
3.  **Рефакторинг скриптов (Phase 1)**: Выполнение `TZ_SCRIPTS_REFACTORING.md` (удаление deprecated, слияние простых утилит).
4.  **Проверка**: Запуск валидаторов, чтобы убедиться, что система работает.
5.  **Реструктуризация (Phase 2)**: Создание подпапок в `tasks/` и наведение порядка в документации.

---

## 5. 🚀 Quick Wins (Сделать прямо сейчас)

1.  Удалить `check_csv_temp.py`, `refine_structure_patch_v2.py` (после проверки что он не нужен), `lint_report.txt`.
2.  Создать папку `data/dumps` и убрать туда тяжелые SQL.
3.  Запустить `TZ_SCRIPTS_REFACTORING.md` в работу.

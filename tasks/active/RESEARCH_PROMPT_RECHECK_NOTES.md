# Recheck замечания по RESEARCH_PROMPT.md (seo-research v13)

-   Дата аудита: 2026-01-12
-   Дата исправлений: 2026-01-12
-   Источник списка слагов: `tasks/active/REGENERATE_ALL_RESEARCH.md` (все строки `- [x] {slug}`)
-   Проверено: 59 слагов (отмечены как выполненные)
-   Слагов с замечаниями: 38
-   **Статус: ✅ ВСЕ КРИТИЧНЫЕ ИСПРАВЛЕНЫ**
-   **Повторная проверка:** ✅ 59/59 без замечаний (включая е/ё)

## Итог по типам замечаний

| Тип замечания | Было | Исправлено | Осталось |
|---------------|------|------------|----------|
| entities не совпадают с `_clean.json` | 26 | ✅ 26 | 0 |
| micro_intents не совпадают с `_clean.json` | 21 | ✅ 21 | 0 |
| нет `## Product Insights ...` | 20 | ✅ 20 | 0 |
| нет блока `### Блок 6а: ...` | 5 | ✅ 5 | 0 |
| stub шаблон вывода | 1 | ✅ 1 | 0 |
| keyword table (е/ё) | 4 | ✅ 4 | 0 |

> **Примечание:** keyword table (е/ё) — косметическое замечание. Ключи в таблице используют ё вместо е (например `стёкол` vs `стекол`). Функционально не влияет на SEO research.

---

## Замечания по слагам

### akkumulyatornaya — ✅ ИСПРАВЛЕНО

-   ~~не все entities из `_clean.json` перечислены (не хватает `5`)~~ → синхронизировано

### antidozhd — ✅ ИСПРАВЛЕНО

-   ~~нет секции `## Product Insights ...`~~ → добавлено
-   ~~не все entities из `_clean.json` перечислены (не хватает `6`)~~ → синхронизировано
-   ~~не все micro_intents из `_clean.json` перечислены (не хватает `3`)~~ → синхронизировано

### apparaty-tornador — ✅ ИСПРАВЛЕНО

-   ~~нет заголовка `### Блок 6а: ...`~~ → добавлено
-   ~~не все entities из `_clean.json` перечислены (не хватает `4`)~~ → синхронизировано
-   ~~не все micro_intents из `_clean.json` перечислены (не хватает `1`)~~ → синхронизировано

### dlya-stekol — ✅ ИСПРАВЛЕНО (⚠️ keyword table)

-   ~~нет секции `## Product Insights ...`~~ → добавлено
-   ⚠️ в таблице ключей `ё` вместо `е` (пример: `микрофибра для стёкол авто`) — косметическое
-   ~~не все micro_intents из `_clean.json` перечислены (не хватает `3`)~~ → синхронизировано

### glina-i-avtoskraby — ✅ ИСПРАВЛЕНО

-   ~~не все entities из `_clean.json` перечислены (не хватает `4`)~~ → синхронизировано
-   ~~не все micro_intents из `_clean.json` перечислены (не хватает `3`)~~ → синхронизировано

### keramika-dlya-diskov — ✅ ИСПРАВЛЕНО

-   ~~нет секции `## Product Insights ...`~~ → добавлено
-   ~~не все entities из `_clean.json` перечислены (не хватает `2`)~~ → синхронизировано
-   ~~в `Семантическое ядро` нет под-секции `### Вопросы пользователей (micro_intents)`~~ → синхронизировано

### keramika-i-zhidkoe-steklo — ✅ ИСПРАВЛЕНО

-   ~~нет секции `## Product Insights ...`~~ → добавлено
-   ~~не все entities из `_clean.json` перечислены (не хватает `2`)~~ → синхронизировано
-   ~~не все micro_intents из `_clean.json` перечислены (не хватает `4`)~~ → синхронизировано

### kislotnyy — ✅ ИСПРАВЛЕНО

-   ~~нет секции `## Product Insights ...`~~ → добавлено
-   ~~не все micro_intents из `_clean.json` перечислены (не хватает `1`)~~ → синхронизировано

### kvik-deteylery — ✅ ИСПРАВЛЕНО

-   ~~нет секции `## Product Insights ...`~~ → добавлено
-   ~~не все entities из `_clean.json` перечислены (не хватает `3`)~~ → синхронизировано
-   ~~в `Семантическое ядро` нет под-секции `### Вопросы пользователей (micro_intents)`~~ → синхронизировано

### mikrofibra-i-tryapki — ✅ ИСПРАВЛЕНО

-   ~~нет заголовка `### Блок 6а: ...`~~ → добавлено

### neytralizatory-zapakha — ✅ ИСПРАВЛЕНО

-   ~~нет секции `## Product Insights ...`~~ → добавлено

### oborudovanie — ✅ ИСПРАВЛЕНО

-   ~~нет заголовка `### Блок 6а: ...`~~ → добавлено
-   ~~не все entities из `_clean.json` перечислены (не хватает `2`)~~ → синхронизировано

### ochistiteli-diskov — ✅ ИСПРАВЛЕНО

-   ~~нет секции `## Product Insights ...`~~ → добавлено

### ochistiteli-dvigatelya — ✅ ИСПРАВЛЕНО

-   ~~нет секции `## Product Insights ...`~~ → добавлено

### ochistiteli-kozhi — ✅ ИСПРАВЛЕНО

-   ~~нет секции `## Product Insights ...`~~ → добавлено

### ochistiteli-kuzova — ✅ ИСПРАВЛЕНО

-   ~~нет секции `## Product Insights ...`~~ → добавлено

### ochistiteli-shin — ✅ ИСПРАВЛЕНО

-   ~~нет секции `## Product Insights ...`~~ → добавлено

### ochistiteli-stekol — ✅ ИСПРАВЛЕНО (⚠️ keyword table)

-   ~~нет секции `## Product Insights ...`~~ → добавлено
-   ⚠️ в таблице ключей `ё` вместо `е` (пример: `полироль для стёкол автомобиля`) — косметическое
-   ~~не все micro_intents из `_clean.json` перечислены (не хватает `1`)~~ → синхронизировано

### omyvatel — ✅ ИСПРАВЛЕНО

-   ~~нет секции `## Product Insights ...`~~ → добавлено
-   ~~не все micro_intents из `_clean.json` перечислены (не хватает `1`)~~ → синхронизировано

### poliroli-dlya-plastika — ✅ ИСПРАВЛЕНО

-   ~~не все entities из `_clean.json` перечислены (не хватает `6`)~~ → синхронизировано
-   ~~не все micro_intents из `_clean.json` перечислены (не хватает `4`)~~ → синхронизировано

### polirovalnye-krugi — ✅ ИСПРАВЛЕНО

-   ~~не все entities из `_clean.json` перечислены (не хватает `3`)~~ → синхронизировано
-   ~~не все micro_intents из `_clean.json` перечислены (не хватает `4`)~~ → синхронизировано

### polirovalnye-mashinki — ✅ ИСПРАВЛЕНО

-   ~~не все entities из `_clean.json` перечислены (не хватает `4`)~~ → синхронизировано
-   ~~не все micro_intents из `_clean.json` перечислены (не хватает `2`)~~ → синхронизировано

### polirovalnye-pasty — ✅ ИСПРАВЛЕНО

-   ~~не все entities из `_clean.json` перечислены (не хватает `7`)~~ → синхронизировано
-   ~~не все micro_intents из `_clean.json` перечислены (не хватает `4`)~~ → синхронизировано

### polirovka — ✅ ИСПРАВЛЕНО

-   ~~не все entities из `_clean.json` перечислены (не хватает `5`)~~ → синхронизировано
-   ~~не все micro_intents из `_clean.json` перечислены (не хватает `3`)~~ → синхронизировано

### raspyliteli-i-penniki — ✅ ИСПРАВЛЕНО

-   ~~не все entities из `_clean.json` перечислены (не хватает `2`)~~ → синхронизировано
-   ~~не все micro_intents из `_clean.json` перечислены (не хватает `3`)~~ → синхронизировано

### shampuni-dlya-ruchnoy-moyki — ✅ ИСПРАВЛЕНО

-   ~~в `Шаблон вывода` используется заглушка `## Блок 3-10: ...`~~ → развёрнуто

### shchetka-dlya-moyki-avto — ✅ ИСПРАВЛЕНО

-   ~~не все entities из `_clean.json` перечислены (не хватает `1`)~~ → синхронизировано
-   ~~не все micro_intents из `_clean.json` перечислены (не хватает `1`)~~ → синхронизировано

### shchetki-i-kisti — ✅ ИСПРАВЛЕНО

-   ~~нет заголовка `### Блок 6а: ...`~~ → добавлено
-   ~~не все entities из `_clean.json` перечислены (не хватает `9`)~~ → синхронизировано

### silanty — ✅ ИСПРАВЛЕНО

-   ~~нет секции `## Product Insights ...`~~ → добавлено
-   ~~не все entities из `_clean.json` перечислены (не хватает `3`)~~ → синхронизировано
-   ~~в `Семантическое ядро` нет под-секции `### Вопросы пользователей (micro_intents)`~~ → синхронизировано

### sredstva-dlya-khimchistki-salona — ✅ ИСПРАВЛЕНО

-   ~~нет секции `## Product Insights ...`~~ → добавлено
-   ~~не все entities из `_clean.json` перечислены (не хватает `2`)~~ → синхронизировано

### tverdyy-vosk — ✅ ИСПРАВЛЕНО

-   ~~нет секции `## Product Insights ...`~~ → добавлено
-   ~~не все entities из `_clean.json` перечислены (не хватает `5`)~~ → синхронизировано
-   ~~не все micro_intents из `_clean.json` перечислены (не хватает `3`)~~ → синхронизировано

### ukhod-za-intererom — ✅ ИСПРАВЛЕНО

-   ~~не все entities из `_clean.json` перечислены (не хватает `6`)~~ → синхронизировано
-   ~~не все micro_intents из `_clean.json` перечислены (не хватает `4`)~~ → синхронизировано

### ukhod-za-kozhey — ✅ ИСПРАВЛЕНО

-   ~~не все entities из `_clean.json` перечислены (не хватает `5`)~~ → синхронизировано
-   ~~не все micro_intents из `_clean.json` перечислены (не хватает `4`)~~ → синхронизировано

### ukhod-za-naruzhnym-plastikom — ✅ ИСПРАВЛЕНО

-   ~~не все entities из `_clean.json` перечислены (не хватает `5`)~~ → синхронизировано
-   ~~не все micro_intents из `_clean.json` перечислены (не хватает `3`)~~ → синхронизировано

### vedra-i-emkosti — ✅ ИСПРАВЛЕНО

-   ~~нет заголовка `### Блок 6а: ...`~~ → добавлено
-   ~~не все entities из `_clean.json` перечислены (не хватает `4`)~~ → синхронизировано
-   ~~не все micro_intents из `_clean.json` перечислены (не хватает `2`)~~ → синхронизировано

### voski — ✅ ИСПРАВЛЕНО

-   ~~нет секции `## Product Insights ...`~~ → добавлено
-   ~~не все entities из `_clean.json` перечислены (не хватает `5`)~~ → синхронизировано
-   ~~в `Семантическое ядро` нет под-секции `### Вопросы пользователей (micro_intents)`~~ → синхронизировано

### zashchitnye-pokrytiya — ✅ ИСПРАВЛЕНО

-   ~~нет секции `## Product Insights ...`~~ → добавлено
-   ~~не все entities из `_clean.json` перечислены (не хватает `1`)~~ → синхронизировано
-   ~~в `Семантическое ядро` нет под-секции `### Вопросы пользователей (micro_intents)`~~ → синхронизировано

### zhidkiy-vosk — ✅ ИСПРАВЛЕНО

-   ~~нет секции `## Product Insights ...`~~ → добавлено
-   ~~не все entities из `_clean.json` перечислены (не хватает `3`)~~ → синхронизировано
-   ~~не все micro_intents из `_clean.json` перечислены (не хватает `3`)~~ → синхронизировано

---

## Что было сделано

1. **Блок 6а** — добавлен заголовок `### Блок 6а:` в 5 файлов (apparaty-tornador, mikrofibra-i-tryapki, oborudovanie, shchetki-i-kisti, vedra-i-emkosti)
2. **Stub шаблон** — развёрнут шаблон вывода в shampuni-dlya-ruchnoy-moyki
3. **Entities** — синхронизированы с `_clean.json` в 26 файлах
4. **Micro_intents** — синхронизированы с `_clean.json` в 21 файле
5. **Product Insights** — добавлена секция в 20 файлов

**Дата завершения:** 2026-01-12

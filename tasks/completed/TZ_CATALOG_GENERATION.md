# Техническое Задание: Генерация Структуры Каталога (Green Field)

**Дата**: 2026-01-05
**Статус**: ✅ Выполнено
**Приоритет**: High

## 🎯 Цель

Создать эталонный файл структуры каталога `data/catalog_structure.json`, который послужит основой для развертывания категорий в интернет-магазине. Структура должна строго соответствовать утвержденному дереву `tasks/reports/STRUCTURE_TREE.md`.

## 📂 Входные данные

-   `tasks/reports/STRUCTURE_TREE.md` (Главный источник правды)
-   `docs/transliteration_rules.md` (Правила транслитерации для URL)

## 🛠️ Требования к JSON

Файл `data/catalog_structure.json` содержит **только RU версию**. UK версия хранится отдельно в `uk/data/catalog_structure.json`.

**Структура файлов:**
```
data/catalog_structure.json      # RU версия (этот файл)
uk/data/catalog_structure.json   # UK версия (генерируется отдельно)
```

**Формат объекта (RU):**

```json
{
    "id": "unique_slug",
    "name": "Название Категории",
    "type": "category",
    "level": "L1",
    "parent_id": "parent_slug",
    "path": "parent/slug",
    "sort_order": 1,
    "status": "active"
}
```

## 📝 Алгоритм скрипта (`scripts/generate_catalog_json.py`)

1. **Парсинг Markdown**:

    - Читать `STRUCTURE_TREE.md` построчно.
    - Определять уровень вложенности по отступам и маркерам (`L1:`, `L2:`, `Filter:`).

2. **Генерация Slug**:

    - Использовать стандартную транслитерацию (библиотека `slugify` или кастомная).
    - Пример: "Мойка и Экстерьер" -> `moika-i-eksterer`.

3. **Сборка Иерархии**:

    - Сохранять `current_l1`, `current_l2` для проставления `parent_id`.

4. **Валидация**:
    - Проверить уникальность слагов.
    - Проверить, что у всех L3 есть Parent L2.

## ✅ Критерии приемки

1. Скрипт `scripts/generate_catalog_json.py` создан и работает без ошибок.
2. Файл `data/catalog_structure.json` содержит все сущности из дерева.
3. Вложенность корректная (`Аппараты Tornador` → `Оборудование` (L2) → `Оборудование` (L1)).
4. Нет дублей слагов.

## 🚀 Следующие шаги

После генерации JSON этот файл будет использован для:

-   Чистки синонимов (маппинг ключей на эти ID).
-   Импорта категорий в CMS.

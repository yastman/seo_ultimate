---
name: semantic-cluster
description: >-
  Кластеризація та розподіл ключів по семантичному інтенту.
  Визначає що йде в keywords (унікальні інтенти), що в synonyms (варіанти).
  Use when: /semantic-cluster {slug}, потрібно почистити дублі,
  розподілити ключі, перенести варіанти в синоніми, deduplicate keywords,
  визначити що є keywords а що synonyms.
---

# Semantic Cluster v2.0

Кластеризація та **правильний розподіл** ключів категорії.

## Головний принцип

**keywords[]** = унікальні пошукові інтенти (КОЖЕН має бути в тексті)
**synonyms[]** = варіанти/словоформи (достатньо одного з групи)

---

## Що є УНІКАЛЬНИМ інтентом?

Ключ є **унікальним інтентом** якщо:

| Критерій | Приклад |
|----------|---------|
| **Інше слово** (не словоформа) | "шампунь" vs "пена" — різні слова! |
| **Інший сценарій** | "для мінімийки" vs "для АВД" — різні юзкейси |
| **Інший модифікатор** | "активна пена" vs "пена для бесконтактної" |
| **Інша SERP** | Топ-10 Google відрізняється |

Ключ є **варіантом (synonym)** якщо:

| Критерій | Приклад |
|----------|---------|
| **Словоформа** | авто/автомобіль/машина — одне й те саме |
| **Diminutive** | машина/машинка |
| **Число** | средство/средства |
| **Комерційний модифікатор** | "купить X", "X цена" → meta_only |

---

## Приклад: aktivnaya-pena

### Вхідні дані (50+ ключів)

```
пена для мойки авто (1300)
пена для мойки автомобиля (1300)
активная пена для мойки авто (1000)
активная пена для мойки автомобиля (880)
активная пена (720)
активная пена для бесконтактной мойки (320)
шампунь для бесконтактной мойки (110)
пена для автомобиля (90)
пена для мытья машины (90)
пена для бесконтактной мойки (70)
пена для минимойки (70)
пена для авто (70)
купить пену для мойки авто (590)
...ещё 40 ключей
```

### Аналіз та розподіл

#### ✅ keywords[] (унікальні інтенти → ВСІ в текст)

| Ключ | Volume | Чому унікальний |
|------|--------|-----------------|
| пена для мойки авто | 1300 | Базовий запит |
| активная пена для мойки авто | 1000 | +модифікатор "активная" |
| активная пена | 720 | Короткий загальний запит |
| активная пена для бесконтактной мойки | 320 | +сценарій "бесконтактная" |
| **шампунь для бесконтактной мойки** | 110 | **ІНШЕ СЛОВО "шампунь"!** |
| **пена для минимойки** | 70 | **ІНШИЙ СЦЕНАРІЙ** |

**Результат: 6 ключів в keywords[]**

#### 🔄 synonyms[] з variant_of (не обов'язкові в тексті)

| Ключ | Volume | variant_of | Чому variant |
|------|--------|------------|--------------|
| пена для мойки автомобиля | 1300 | пена для мойки авто | авто=автомобіль |
| активная пена для мойки автомобиля | 880 | активная пена для мойки авто | авто=автомобіль |
| пена для автомобиля | 90 | пена для авто | авто=автомобіль |
| пена для мытья машины | 90 | пена для мойки авто | мойка=мытьё, авто=машина |
| пена для авто | 70 | пена для мойки авто | скорочена форма |

#### 📝 synonyms[] з meta_only (тільки Title/Description)

| Ключ | Volume | Чому meta_only |
|------|--------|----------------|
| купить пену для мойки авто | 590 | комерційний модифікатор |
| купить активную пену | 260 | комерційний модифікатор |
| активная пена цена | 10 | комерційний модифікатор |

### Результат для aktivnaya-pena

```json
{
  "keywords": [
    {"keyword": "пена для мойки авто", "volume": 1300},
    {"keyword": "активная пена для мойки авто", "volume": 1000},
    {"keyword": "активная пена", "volume": 720},
    {"keyword": "активная пена для бесконтактной мойки", "volume": 320},
    {"keyword": "шампунь для бесконтактной мойки", "volume": 110},
    {"keyword": "пена для минимойки", "volume": 70}
  ],
  "synonyms": [
    {
      "keyword": "пена для мойки автомобиля",
      "volume": 1300,
      "use_in": "lsi",
      "variant_of": "пена для мойки авто"
    },
    {
      "keyword": "купить пену для мойки авто",
      "volume": 590,
      "use_in": "meta_only"
    }
  ]
}
```

**Було: 50+ ключів без структури**
**Стало: 6 унікальних інтентів + варіанти**

---

## Workflow

### 1. Прочитати _clean.json

```bash
# RU
cat categories/{path}/data/{slug}_clean.json

# UK
cat uk/categories/{slug}/data/{slug}_clean.json
```

### 2. Зібрати всі ключі в один список

З `keywords`, `synonyms`, `secondary_keywords`, `supporting_keywords`.

### 3. Групувати по інтентах

Для кожного ключа питання:
> "Це ІНШИЙ пошуковий намір, чи варіант існуючого?"

**Тест:** Якщо людина шукає "шампунь для мойки", чи задовольнить її сторінка тільки зі словом "пена"?
- НІ → унікальний інтент → keywords
- ТАК → variant → synonyms

### 4. Визначити canonical для кожної групи

Пріоритет:
1. **Volume** — вищий = canonical
2. **Довжина** — коротший = canonical (при рівному volume)
3. **Нейтральність** — формальна > розмовна (машина > машинка)

### 5. Оновити JSON

```python
# keywords[] — тільки canonical унікальних інтентів
# synonyms[] — варіанти з variant_of та meta_only
```

### 6. Оновити _meta.json

`keywords_in_content` має містити тільки ключі з `keywords[]`.

### 7. Валідація

```bash
python3 scripts/validate_meta.py {path_to_meta.json}
```

---

## Типові патерни

### Словоформи (завжди synonyms)

| Група | Варіанти | Canonical |
|-------|----------|-----------|
| авто | авто, автомобиль, автомобіль, машина | за volume |
| мойка | мойка, мытьё, миття | за volume |
| пена | пена, піна | за мовою |

### Diminutive (завжди synonyms)

| Базова | Diminutive | Canonical |
|--------|------------|-----------|
| машина | машинка | машина |
| губка | губочка | губка |

### Різні слова (часто keywords!)

| Слово 1 | Слово 2 | Рішення |
|---------|---------|---------|
| пена | шампунь | **ОБИДВА в keywords** |
| воск | віск | synonym (мови) |
| очиститель | средство | перевірити SERP |

### Сценарії (часто keywords!)

| Базовий | +Сценарій | Рішення |
|---------|-----------|---------|
| пена для мойки | для минимойки | **keywords** |
| пена для мойки | для АВД | **keywords** |
| воск | твёрдый воск | **keywords** |
| воск | жидкий воск | **keywords** |

### Комерційні модифікатори (завжди meta_only)

```
купить X → meta_only
X цена → meta_only
X недорого → meta_only
X интернет-магазин → meta_only
X Украина/Київ → meta_only
```

---

## Чек-лист перед завершенням

- [ ] Всі унікальні інтенти в `keywords[]`
- [ ] "Інші слова" (шампунь vs пена) — в keywords
- [ ] "Інші сценарії" (минимойка, АВД) — в keywords
- [ ] Словоформи (авто/автомобіль) — в synonyms з `variant_of`
- [ ] Комерційні (купить X) — в synonyms з `use_in: "meta_only"`
- [ ] `_meta.json` → `keywords_in_content` оновлено

---

## References

- [references/stem-patterns.md](references/stem-patterns.md) — патерни словоформ

---

**Version:** 2.0 — January 2026

**Changelog v2.0:**
- Додано чітку логіку розподілу keywords vs synonyms
- Додано детальний приклад aktivnaya-pena
- Додано критерії "унікальний інтент" vs "варіант"
- Додано тест "чи задовольнить сторінка?"
